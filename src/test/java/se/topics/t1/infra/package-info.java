/**
 * Infrastructure code authored by us: PICT runners, model parsers, result
 * aggregators, metamorphic-test generators, HTML report helpers.
 *
 * Per the assignment (section 13) we must write tests for our own
 * infrastructure. Production-side helpers used by tests live here so they
 * can be unit-tested under {@code infra} alongside other test code.
 */
package se.topics.t1.infra;
